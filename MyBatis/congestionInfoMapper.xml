<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Welcome to use the TableGo Tools.
    
    http://vipbooks.iteye.com
    http://blog.csdn.net/vipbooks
    http://www.cnblogs.com/vipbooks
    
    Author:bianj
    Email:edinsker@163.com
    Version:5.8.8
-->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- CONGESTION_INFO -->
<mapper namespace="ABCDEFG.congestionInfo">
    <!-- 结果映射 -->
    <resultMap id="congestionInfoMap" type="CongestionInfoEntity"></resultMap>
    
    <!-- 表所有字段 -->
    <sql id="allColumns">
        ID, INFO_ID, MSG_TYPE, DEVNO, DETECTION_TIME, CHANNELEVT_INFOID, TIME
    </sql>
    
    <!-- 批量插入数据 -->
    <insert id="insertCongestionInfos" parameterType="list">
        INSERT INTO CONGESTION_INFO (<include refid="allColumns" />)
        VALUES (
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.infoId},
                #{item.msgType},
                #{item.devno},
                #{item.detectionTime},
                #{item.channelevtInfoid},
                #{item.time}
            )
        </foreach>
    </insert>
    
</mapper>